# This is a comment
# Basic grammar 

variable_int = 1
variable_string = "a string, how exciting!"
variable_bool = true

function -> arga {
  puts(arga)
}

function('hello world!')

add -> a, b {
  return a + b
}

puts(add(1, 2))

# Object stuff

1.add 2 # >

hash = Hash.new(meow: "foo")